<Window x:Class="ProSchedules.UI.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:ProSchedules.UI"
        xmlns:models="clr-namespace:ProSchedules.Models"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Filter" Height="600" Width="700"
        MinWidth="600" MinHeight="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FF1F1F1F" />
                    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#FF292929" />
                    <SolidColorBrush x:Key="BorderBrush" Color="#FF444444" />
                    <SolidColorBrush x:Key="ForegroundBrush" Color="#EEEEEE" />
                </ResourceDictionary>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/ProSchedules;component/UI/Themes/ElementStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Editable ComboBox style for filter value input -->
            <Style x:Key="EditableValueComboBoxStyle" TargetType="{x:Type ComboBox}">
                <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="IsEditable" Value="True"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <Border x:Name="Border"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="PART_EditableTextBox"
                                                 Grid.Column="0"
                                                 Margin="8,4"
                                                 Foreground="{DynamicResource ForegroundBrush}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 CaretBrush="{DynamicResource ForegroundBrush}"
                                                 VerticalAlignment="Center"
                                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                        <ToggleButton Grid.Column="1"
                                                      Focusable="False"
                                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ClickMode="Press"
                                                      Background="Transparent"
                                                      BorderThickness="0"
                                                      Width="24">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent">
                                                        <materialDesign:PackIcon Kind="ChevronDown"
                                                                                 Width="16" Height="16"
                                                                                 Foreground="{DynamicResource ForegroundBrush}"
                                                                                 HorizontalAlignment="Center"
                                                                                 VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>
                                    </Grid>
                                </Border>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="200">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="{x:Type ComboBoxItem}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF3A3A3A"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FF2D5F8D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#1E1E1E" 
            BorderBrush="#333333" 
            BorderThickness="1" 
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <ui:TitleBar Grid.Row="0" Foreground="{DynamicResource ForegroundBrush}" IsMinimizeVisible="False" />

            <!-- Header -->
            <Grid Grid.Row="1" Margin="20,10,20,10">
                <TextBlock Text="Filter Rules" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                <Button HorizontalAlignment="Right" 
                        Click="AddFilterRule_Click"
                        Width="32" Height="32"
                        Style="{StaticResource FlatIconButton}"
                        ToolTip="Add Filter Rule">
                    <materialDesign:PackIcon Kind="Plus" Width="24" Height="24" Foreground="White"/>
                </Button>
            </Grid>

            <!-- Filter Rules -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
                <ItemsControl ItemsSource="{Binding FilterCriteria}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#252525" BorderBrush="#333333" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Filter by:" Foreground="Gray" VerticalAlignment="Center" Grid.Column="0"/>

                                    <!-- Column selector -->
                                    <ComboBox Grid.Column="1"
                                              ItemsSource="{Binding DataContext.AvailableFilterColumns, RelativeSource={RelativeSource AncestorType=Window}}"
                                              SelectedItem="{Binding SelectedColumn}"
                                              Style="{StaticResource DarkComboBoxStyle}"
                                              Margin="0,0,6,0"/>

                                    <!-- Condition selector -->
                                    <ComboBox Grid.Column="2"
                                              ItemsSource="{x:Static models:FilterItem.Conditions}"
                                              SelectedItem="{Binding SelectedCondition}"
                                              Style="{StaticResource DarkComboBoxStyle}"
                                              Margin="0,0,6,0"/>

                                    <!-- Value: editable ComboBox -->
                                    <ComboBox Grid.Column="3"
                                              ItemsSource="{Binding AvailableValues}"
                                              Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                              IsEditable="True"
                                              Style="{StaticResource EditableValueComboBoxStyle}"
                                              Margin="0,0,6,0"/>

                                    <!-- Delete -->
                                    <Button Grid.Column="4" Click="RemoveFilterRule_Click" 
                                            Style="{StaticResource FlatIconButton}" Width="24" Height="24">
                                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="Gray"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button Content="Apply" Click="FilterApply_Click" 
                        Style="{StaticResource CustomActionButtonStyle}" 
                        Width="120" Height="35" FontSize="12" Margin="5,0,5,0"/>
                <Button Content="Clear" Click="FilterClear_Click" 
                        Style="{StaticResource CustomActionButtonStyle}" 
                        Width="120" Height="35" FontSize="12" Margin="5,0,5,0"/>
                <Button Content="Cancel" Click="FilterCancel_Click" 
                        Style="{StaticResource NegativeActionButtonStyle}" 
                        Width="120" Height="35" FontSize="12" Margin="5,0,5,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
